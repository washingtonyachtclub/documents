name: Build PDFs

on:
  push:
    branches:
      - main
  workflow_dispatch:

defaults:
  run:
    working-directory: ./pdf-generator

jobs:
  build-pdfs:
    runs-on: ubuntu-latest
    steps:
    - name: Pull repository files
      uses: actions/checkout@v6

    # Temporary workaround for Ubuntu 23.10+ AppArmor restrictions
    - name: Enable unprivileged user namespaces
      run: echo 0 | sudo tee /proc/sys/kernel/apparmor_restrict_unprivileged_userns

    - name: Load and cache Node
      uses: actions/setup-node@v6
      with:
        node-version: '24'
        cache: 'npm'
        cache-dependency-path: 'pdf-generator/package-lock.json'

    - name: Install Dependencies
      run: npm ci

    - name: Build Constitution
      run: npm run build:constitution

    - name: Build By-Laws
      run: npm run build:bylaws

    - name: Build Policies
      run: npm run build:policies

    - name: Build Windsurf Policies
      run: npm run build:windsurf-policies

    - name: Build Windsurf Badge Program
      run: npm run build:windsurf-badges

    - name: Build Windsurf SSP Southerly Policies
      run: npm run build:windsurf-southerly

    - name: Archive PDFs
      uses: actions/upload-artifact@v6
      with:
        name: wyc-doc-pdfs-${{ github.run_id }}
        path: |
          *.pdf

    - name: Download PDFs
      uses: actions/download-artifact@v7
      with:
        name: wyc-doc-pdfs-${{ github.run_id }}

    - name: Notify Success
      if: success()
      run: echo "PDF build completed successfully."

    - name: Notify Failure
      if: failure()
      run: echo "PDF build failed. Check the logs for details."
